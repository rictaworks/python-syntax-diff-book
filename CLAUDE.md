# Claude Safety Rules

## 削除系コマンドの禁止（重要）

以下のルールはこのワークスペース内のすべての会話で絶対に守られる：

- Claude はファイルまたはディレクトリを削除するコマンドを一切生成してはならない。
  例：rm, rm -rf, rm *, rmdir, unlink, cache --delete,
      lftp mirror --delete, rsync --delete, git clean -df, find -delete 等。

- 削除が必要な場合でも、Claude は削除コマンドを提案せず、
  「手動で削除してください」といった説明に留めること。

- 削除の推奨・削除操作の自動判断も禁止。

- ssh / lftp / デプロイ系スクリプトを生成する場合でも、
  削除コマンドの生成は禁止。

これらはすべての会話・コード生成に適用される。

## シークレット管理（重要）

- `config/master.key` など機密ファイルを `git add` するコードを生成してはならない
- デプロイスクリプト・セットアップ手順でも同様
- シークレットは必ず環境変数（RAILS_MASTER_KEY 等）で渡すこと
- `.gitignore` への追加を確認する手順を必ずコードに含めること
- 初回コミット前に `git status` でステージング確認を促すこと

## プロジェクト概要

- 本リポジトリは電子書籍（Kindle・リフロー型EPUB）「差分で読むPython文法」デモ版（試し読み版）の制作プロジェクトである。Webアプリケーションではない。
- 詳細な要件・章構成・読者ペルソナ・検証ループは `requirements.md` を参照する（同ファイルは `.gitignore` 対象のためリポジトリのクローン先には存在しない場合がある）。
- 対象Pythonバージョンは3.13系（パッチバージョンまでdevcontainerとロックファイルでピン留めする方針。devcontainer未整備の場合はENV/DEVELOPMENT.mdのTODOを確認する）。

## 開発フロー（TDD）

- 本文中のPythonコードリストは以下の順序で作成する：**plan → red test → coding → green test**。
  1. plan: どのコードリストで何を示すか（差分・挙動）を明確化する
  2. red test: 期待する出力・例外を pytest でまず記述し、失敗させる
  3. coding: コードリストを実装する
  4. green test: pytest を対象Pythonバージョンで通す
- 実行ログ・エラーメッセージは実際に動かして観測したものだけを掲載する。公式ドキュメントは裏取りにのみ使用する（requirements.md 2.2参照）。

## ブランチ・PR運用

- `main` ブランチでの直接作業は禁止する（フィーチャーブランチで作業する）。
- `code/`（掲載コードリスト）・`build/`（ビルドスクリプト）配下の変更は必ずPRを作成する（実体パスは`requirements.md` §12のリポジトリ構成に従う）。
- それ以外（`manuscript/`・`figures/`・SPEC・設定ファイル等）の変更は `main` への直接pushを許可する。

## ディレクトリ規約

- 制作物の実体パス（`manuscript/` `code/` `figures/` `logs/` `refs/` `styles/` `build/` `.devcontainer/`）は `requirements.md` §12「リポジトリ構成」を正とする。ここに記載のない新しいパス（`src/` 等）を作らない。
- `TASKS/` タスク管理
- `DEBUG/` バグ・不具合報告
- `CLIENT/` クライアント（著者・編集）要望
- `WORK/` 作業報告
- `ENV/DEVELOPMENT.md` 開発環境（devcontainer・Codespaces・バージョン固定）
- `ENV/PRODUCTION.md` 本番環境（このプロジェクトでは「KDP公開時の要件」を指す。稼働サーバは存在しない）
- `SPEC/` 仕様書・図解
- `DELETE/` ゴミ箱。削除が必要なファイルは `rm` 等で消さず、このディレクトリへ移動する（上記「削除系コマンドの禁止」ルールの受け皿）
- `app-ui/` デザイン参照専用（表紙・図版のモック）。編集スコープに含めない（`.claude/Manager.md` 参照）

## 図解方針

- `SPEC/` にmermaidで図解を作成・更新する。
- 本プロジェクトはソフトウェアの実装を持たないため、ER図・DFD・シーケンス図・クラス図・状態遷移図・ユースケース図は対象外とする。
- 代わりに「章依存図」（`requirements.md` の目次構成表の依存章列）と「編集ループ図」（v1→v2→v3の欠陥解消の遷移）をmermaidで管理する。

## 画像・図版方針

- 表紙・図版はAI生成画像を用いる方針とする（`app-ui/` に配置し、`designer` エージェントが `.claude/CRAP.md` に基づきレビューする）。

## コーディング規約（ビルドスクリプト・コードリスト共通）

- 制御構文・条件構文以外の処理は関数またはクラスに書く。
- セキュリティの観点からグローバル変数を禁止する。
- 文字列リテラルは設定ファイル（またはデータ）に分離し、ハードコードがないことを確認するテストを書く。
- フォールバック処理は禁止する。例外は握りつぶさず、明示的に処理する。
- デバッグトレースができるようにログを出力するコードを書く。
- アイコンはFontAwesomeを使用する。絵文字は使用しない。
- メンテナンスコストとセキュリティの観点から、実績のあるライブラリ・OSS（例：EPUB生成のPandoc）を優先し、車輪の再発明を避けてオリジナルコードを少なく保つ（`.claude/development-principles.md` のYAGNI/KISSと合わせて適用する）。

## CI/CD

- CIは必須：pytestによるコードリストの検証、EPUBビルドの検証を行う。
- CD（KDPへの提出等の公開作業）はClaude Desktop側で別途設定するため、本リポジトリのCIには含めない。

## コミット前レビュー

- コミット前に `/security-review` を実行する。
- 該当する場合は `.claude/QC10.md`（品質管理10項目）・`.claude/TM.md`（テストメソッド）・`.claude/OWASP10.md`・`.claude/CC.md`（コンプライアンス10項目）の該当項目を確認する。

## PRの記載事項

- PRのタイトル・本文は日本語で記載する。
- PRには、非エンジニア（編集者・読者視点）向けに「この変更で何が読みやすくなったか／どう検証したか」をユーザーテスト手順として平易な言葉で丁寧に記載する（`pr-checker` 参照）。
- 記載したユーザーテスト手順は `tester` が `test/pr<PR番号>/` に実行スクリプトを作成する入力になる。

## エージェント構成

- 本プロジェクトの規模（デモ版・小規模）に応じて、以下のエージェントを `.claude/agents/` に定義する：`writer`（原稿執筆）・`tester`（コードリスト検証・全PRのユーザーテスト手順の実行スクリプト化）・`designer`（図版・EPUBスタイル）・`deployer`（EPUBビルド）・`project-manager`（既存 `.claude/Manager.md` のIssue分割・依存管理ロジックを採用）・`pr-checker`（PRタイトル・本文の日本語化とユーザーテスト手順の追記。コードレビューは行わない）。
- `director`・`data-scientist`・`service-manager` は稼働サービス・データ分析の対象がないため今回は割愛する（必要になれば追加する）。