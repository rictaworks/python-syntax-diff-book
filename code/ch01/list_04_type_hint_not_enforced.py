"""リスト1-4: 型ヒントは実行時に強制されない。

`a: int` のような注釈は、静的チェッカやIDEのための
メタデータに過ぎず、Python自体は実行時に型を検査しない。
型ヒントの詳細な使い方は第6章で扱う。
"""


def add(a: int, b: int) -> int:
    """型ヒント上はintだが、実際には何を渡しても動く。"""
    return a + b
